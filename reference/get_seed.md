# Get the seed of an `n2kModel` object

Get the seed of an `n2kModel` object

## Usage

``` r
get_seed(x)

# S4 method for class 'n2kAnalysisMetadata'
get_seed(x)
```

## Arguments

- x:

  the `n2kModel` object

## Value

the seed of the object
