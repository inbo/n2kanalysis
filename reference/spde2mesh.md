# Convert an `Spde` object to a mesh object

Convert an `Spde` object to a mesh object

## Usage

``` r
# S4 method for class 'Spde'
spde2matern(object)

spde2mesh(object)

# S4 method for class 'Spde'
spde2mesh(object)
```

## Arguments

- object:

  The `Spde` object
